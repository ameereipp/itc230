/*const Game = require("../models/game");


//insert a new doc into the db

Game.countDocuments((err, result) => {
    console.log(result);
});

//find all docs
Game.find({}, (err, result) => {
    //output error of one occurred
    if(err) {
        console.log(err);
    }else{
        //otherwise output the array of docs
        console.log(result);
    }
});*/